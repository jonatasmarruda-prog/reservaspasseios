import {createRemoteJWKSet,jwtVerify} from 'jose';
import webpush from 'web-push';

const PROJECT_ID='trilheiros-reservas';
const OWNER_EMAIL='trilheiros.roomt@gmail.com';
const EMAIL_FROM='Trilheiros de Rondonópolis <reservas@trilheirosderondonopolis.com.br>';
const REPLY_TO='trilheiros.roomt@gmail.com';
const LOGO='https://i.postimg.cc/JnF2F9Hw/LOGO-TRILHEIROS-Photoroom.png';
const WHATSAPP='5566996926174';
const ALLOWED_ORIGINS=new Set(['https://trilheiros-reservas.web.app','https://trilheiros-reservas.firebaseapp.com']);
const JWKS=createRemoteJWKSet(new URL('https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com'));
const clean=v=>String(v??'').trim();
const num=v=>Math.max(0,Number(v||0)||0);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(num(v));
const validEmail=v=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(clean(v));
const balanceOf=s=>{const total=num(s?.sale_total||s?.total_amount||s?.amount),paid=num(s?.paid_amount||s?.amount_paid||s?.received_amount),raw=Number(s?.balance_due);return Number.isFinite(raw)?Math.max(0,raw):Math.max(0,total-paid)};
const fullyPaid=s=>{const total=num(s?.sale_total||s?.total_amount||s?.amount),paid=num(s?.paid_amount||s?.amount_paid||s?.received_amount),balance=balanceOf(s),status=clean(s?.payment_status||s?.status).toLowerCase();return clean(s?.sale_status).toLowerCase()!=='cancelled'&&balance<=0.009&&(['paid','confirmed','approved','completed','pago','quitado'].includes(status)||s?.payment_confirmed===true||(total>0&&paid>=total-0.009))};
const brDate=v=>{const s=clean(v).slice(0,10);if(!/^\d{4}-\d{2}-\d{2}$/.test(s))return'Data a confirmar';const[y,m,d]=s.split('-');return`${d}/${m}/${y}`};
const response=(statusCode,body,origin='')=>({statusCode,headers:{'Content-Type':'application/json','Cache-Control':'no-store','Access-Control-Allow-Origin':ALLOWED_ORIGINS.has(origin)?origin:'https://trilheiros-reservas.web.app','Access-Control-Allow-Headers':'Authorization, Content-Type','Access-Control-Allow-Methods':'POST, OPTIONS','Vary':'Origin'},body:JSON.stringify(body)});

async function verifyOwner(event){const auth=clean(event.headers?.authorization||event.headers?.Authorization);if(!auth.startsWith('Bearer '))throw Object.assign(new Error('Não autenticado'),{status:401});const token=auth.slice(7);const{payload}=await jwtVerify(token,JWKS,{issuer:`https://securetoken.google.com/${PROJECT_ID}`,audience:PROJECT_ID});if(clean(payload.email).toLowerCase()!==OWNER_EMAIL||payload.email_verified===false)throw Object.assign(new Error('Acesso não autorizado'),{status:403});return payload}
function participants(s){const list=Array.isArray(s?.participants)?s.participants:[],names=list.map(x=>clean(x?.full_name||x?.name)).filter(Boolean),fallback=clean(s?.customer_name||s?.responsible_name);return names.length?names:(fallback?[fallback]:[])}
function paymentMethodLabel(v){const s=clean(v).toLowerCase();if(s.includes('parcel')||s.includes('install'))return'PIX parcelado';if(s.includes('card')||s.includes('cart'))return'Cartão de crédito';if(s.includes('pix'))return'PIX';return clean(v)||'Pagamento'}

function paymentTemplate(s){
 const name=clean(s.customer_name||s.responsible_name)||participants(s)[0]||'Trilheiro',first=name.split(/\s+/)[0]||'Trilheiro',trip=clean(s.trip_name)||'seu próximo passeio',destination=clean(s.destination),names=participants(s),date=brDate(s.trip_date),total=num(s.sale_total),paid=num(s.paid_amount),balance=balanceOf(s),last=num(s.last_payment_amount)||paid,protocol=clean(s.protocol)||'—',method=paymentMethodLabel(s.payment_method),quitado=fullyPaid(s),wa=`https://wa.me/${WHATSAPP}?text=${encodeURIComponent(`Olá Jonatas! Estou falando sobre o passeio ${trip}. Protocolo: ${protocol}.`)}`;
 const subject=quitado?`✅ Pagamento quitado: ${trip}`:`💳 Pagamento recebido: ${trip}`;
 const headline=quitado?'Pagamento confirmado e quitado':'Pagamento parcial confirmado';
 const statusText=quitado?'Sua vaga está confirmada e o pagamento está quitado.':`Recebemos ${money(last)}. Ainda falta ${money(balance)} para concluir o pagamento.`;
 const reminder=quitado?'Mais perto da data você receberá as orientações pré-trilha.':'Por favor, conclua o pagamento antes do passeio para manter sua situação financeira regularizada.';
 const html=`<!doctype html><html><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"></head><body style="margin:0;background:#eef4f1;font-family:Arial,Helvetica,sans-serif;color:#17372d"><table role="presentation" width="100%" cellpadding="0" cellspacing="0"><tr><td align="center" style="padding:24px 12px"><table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="max-width:640px;background:#fff;border:1px solid #d9e5df;border-radius:20px"><tr><td align="center" bgcolor="#073226" style="padding:28px 24px;background:#073226"><img src="${LOGO}" width="145" height="145" alt="Trilheiros de Rondonópolis" style="display:block;object-fit:contain"><p style="font-size:12px;color:#d9b44c;font-weight:800">${esc(headline.toUpperCase())}</p><h1 style="font-size:28px;color:#fff;margin:8px 0 0">Olá, ${esc(first)}!</h1></td></tr><tr><td style="padding:26px 24px"><h2 style="font-size:22px;color:#073226">${esc(trip)}</h2>${destination?`<p style="color:#657b72">📍 ${esc(destination)}</p>`:''}<p><b>Data:</b> ${esc(date)}</p><p><b>Participante(s):</b> ${esc(names.length?names.join(' • '):name)}</p><p><b>Forma de pagamento:</b> ${esc(method)}</p><p><b>Valor recebido agora:</b> ${esc(money(last))}</p><p><b>Total já recebido:</b> ${esc(money(paid))}</p><p><b>Valor total:</b> ${esc(money(total))}</p><p style="font-size:17px;color:${quitado?'#13744e':'#9a6500'}"><b>${quitado?'✅ Quitado':`⏳ Falta pagar: ${esc(money(balance))}`}</b></p><p style="color:#50685e">${esc(statusText)}</p><p style="color:#50685e"><b>${esc(reminder)}</b></p><p><b>Protocolo:</b> ${esc(protocol)}</p><table role="presentation" align="center"><tr><td bgcolor="#0b684b" style="border-radius:12px"><a href="${esc(wa)}" style="display:inline-block;padding:14px 22px;color:#fff;text-decoration:none;font-weight:800">Falar com Jonatas no WhatsApp</a></td></tr></table><p style="font-size:12px;color:#71837c;text-align:center">Trilheiros de Rondonópolis • Aqui ninguém vai só. 🥾💚</p></td></tr></table></td></tr></table></body></html>`;
 const text=`Olá, ${first}!\n\n${headline} — ${trip}\nData: ${date}\nForma de pagamento: ${method}\nValor recebido agora: ${money(last)}\nTotal já recebido: ${money(paid)}\nValor total: ${money(total)}\n${quitado?'Pagamento quitado.':`Falta pagar: ${money(balance)}.`}\n\n${reminder}\nProtocolo: ${protocol}\n\nDúvidas: Jonatas — (66) 99692-6174.`;
 return{subject,html,text,quitado,last,balance};
}

async function sendEmail(sale,saleId){
 const email=clean(sale.customer_email||sale.email).toLowerCase(),paid=num(sale.paid_amount),last=num(sale.last_payment_amount);
 if(!validEmail(email))return{sent:false,skipped:true,reason:'invalid_email'};
 if(paid<=0&&last<=0)return{sent:false,skipped:true,reason:'no_confirmed_payment'};
 const key=process.env.RESEND_API_KEY;if(!key)throw new Error('RESEND_API_KEY ausente');
 const t=paymentTemplate(sale),idempotency=`payment-${saleId}-${Math.round(paid*100)}-${Math.round(t.last*100)}`;
 const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','Idempotency-Key':idempotency},body:JSON.stringify({from:EMAIL_FROM,to:[email],reply_to:REPLY_TO,subject:t.subject,html:t.html,text:t.text})});
 const raw=await r.text();if(!r.ok)throw new Error(`Resend ${r.status}: ${raw.slice(0,300)}`);let data={};try{data=JSON.parse(raw)}catch{}
 return{sent:true,to:email,id:data.id||'',kind:t.quitado?'paid':'partial',balance:t.balance,last_payment_amount:t.last};
}

async function sendPush(sale,subscription,saleId){if(!subscription?.endpoint||!subscription?.keys?.p256dh||!subscription?.keys?.auth)return{sent:false,skipped:true,reason:'no_subscription'};const pub=process.env.WEB_PUSH_PUBLIC_KEY,priv=process.env.WEB_PUSH_PRIVATE_KEY;if(!pub||!priv)throw new Error('Chaves Web Push ausentes');webpush.setVapidDetails(`mailto:${OWNER_EMAIL}`,pub,priv);const paid=num(sale.paid_amount),balance=balanceOf(sale),delta=num(sale.last_payment_amount)||paid,name=clean(sale.customer_name||sale.responsible_name)||'Cliente',trip=clean(sale.trip_name)||'Passeio',title=balance<=0.009?`✅ Pagamento confirmado — ${name}`:`💳 Pagamento parcial — ${name}`,body=balance<=0.009?`${trip} • recebido ${money(delta)} • quitado`:`${trip} • recebido ${money(delta)} • falta ${money(balance)}`,payload=JSON.stringify({title,body,url:'/admin?tab=pending',tag:`payment-${saleId}-${Math.round(paid*100)}`,type:'payment_confirmed',timestamp:Date.now()});try{await webpush.sendNotification(subscription,payload,{TTL:3600,urgency:'high'});return{sent:true}}catch(e){const code=Number(e?.statusCode||0);if(code===404||code===410)return{sent:false,expired:true};throw e}}

export const handler=async event=>{const origin=clean(event.headers?.origin);if(event.httpMethod==='OPTIONS')return response(204,{},origin);if(event.httpMethod!=='POST')return response(405,{ok:false,error:'Método não permitido'},origin);if(origin&&!ALLOWED_ORIGINS.has(origin))return response(403,{ok:false,error:'Origem não autorizada'},origin);try{await verifyOwner(event);const body=JSON.parse(event.body||'{}'),sale=body.sale||{},saleId=clean(body.saleId);if(!saleId)throw Object.assign(new Error('Venda não informada'),{status:400});const[email,push]=await Promise.allSettled([sendEmail(sale,saleId),sendPush(sale,body.subscription,saleId)]),emailResult=email.status==='fulfilled'?email.value:{sent:false,error:String(email.reason?.message||email.reason||'Falha no e-mail')},pushResult=push.status==='fulfilled'?push.value:{sent:false,error:String(push.reason?.message||push.reason||'Falha na notificação')};if(!emailResult.sent&&!emailResult.skipped&&!pushResult.sent&&!pushResult.skipped&&!pushResult.expired)return response(502,{ok:false,error:`E-mail: ${emailResult.error||'falhou'}; Push: ${pushResult.error||'falhou'}`,email:emailResult,push:pushResult},origin);return response(200,{ok:true,email:emailResult,push:pushResult},origin)}catch(e){console.error('payment-confirmed',e);return response(e.status||500,{ok:false,error:e.message||'Falha no disparo imediato'},origin)}};
